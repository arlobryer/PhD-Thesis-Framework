"""
This SConscript may contain some further build options for the chapters.
Right now it is just an empty dummy, but it may take stuff at a later
time.
"""

# Get the passed in environment.
Import('env')

# Pass back the modified environment.
Return('env')
